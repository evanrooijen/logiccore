{
  "$schema": "./node_modules/turbo/schema.json",
  "ui": "tui",
  "tasks": {
    "//#fix": {},
    "//#check": {},
    "build": {
      "dependsOn": ["^build"],
      "env": ["DB_FILE_NAME", "SPACETIMEDB_HOST", "SPACETIMEDB_DB_NAME"],
      "outputs": [".next/*"]
    },
    "dev": {
      "env": ["DB_FILE_NAME", "SPACETIMEDB_HOST", "SPACETIMEDB_DB_NAME"],
      "persistent": true,
      "cache": false
    },
    "test": {}
  }
}
